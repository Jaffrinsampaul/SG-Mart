import 'package:flutter/material.dart';
import 'package:sgmart/screen/landing_page.dart';

class Flour_Sooji extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SingleChildScrollView(
        child: Column(
          children: [
            // Home(),
            Text("Flour & Sooji")
          ],
        ),
      ),
    );
  }
}
